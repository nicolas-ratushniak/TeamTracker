<UserControl x:Class="TeamTracker.Wpf.ViewComponents.TeamsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TeamTracker.Wpf.Views"
             xmlns:viewModels="clr-namespace:TeamTracker.Wpf.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.Resources>
        <Style x:Key="TeamListBox" TargetType="ListBox">
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate DataType="{x:Type viewModels:TeamListItemViewModel}">
                        <TextBlock Text="{Binding Path=FullName}" 
                                   FontSize="16" Padding="5"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Border BorderBrush="Black" BorderThickness="1" >
        <ListBox ItemsSource="{Binding Teams}"
                 SelectedItem="{Binding SelectedTeam}"
                 SelectionMode="Single"
                 Style="{StaticResource TeamListBox}"/>
    </Border>
</UserControl>
